name: alos-staging

services:
  caddy:
    image: ${CADDY_IMAGE:-caddy:2.10-alpine}
    environment:
      WEB_HOSTNAME: ${WEB_HOSTNAME:?WEB_HOSTNAME wajib diisi}
      API_HOSTNAME: ${API_HOSTNAME:?API_HOSTNAME wajib diisi}
    ports:
      - "${HTTP_PORT:-80}:80"
      - "${HTTPS_PORT:-443}:443"
    networks: [edge]
    volumes:
      - ../../reverse-proxy/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      web:
        condition: service_healthy
      backend:
        condition: service_healthy
    restart: unless-stopped

  web:
    image: ${ALOS_WEB_IMAGE:?ALOS_WEB_IMAGE wajib diisi}
    environment:
      NEXT_PUBLIC_ALOS_API_BASE_URL: https://${API_HOSTNAME}
      ALOS_BACKEND_INTERNAL_URL: http://backend:8000
    expose: ["3000"]
    networks: [edge]
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://127.0.0.1:3000').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 20s
    restart: unless-stopped

  backend:
    image: ${ALOS_BACKEND_IMAGE:?ALOS_BACKEND_IMAGE wajib diisi}
    environment:
      APP_ENV: staging
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
      DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      GENESIS_BASE_URL: http://genesis:8100
      GENESIS_INTERNAL_TOKEN: ${GENESIS_INTERNAL_TOKEN:?GENESIS_INTERNAL_TOKEN wajib diisi}
      CORS_ALLOWED_ORIGINS: https://${WEB_HOSTNAME:?WEB_HOSTNAME wajib diisi}
      ALOS_CONTRACTS_PATH: /contracts
      OTEL_SERVICE_NAME: alos-backend-staging
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
      OBJECT_STORAGE_ENDPOINT: ${OBJECT_STORAGE_ENDPOINT:-}
      OBJECT_STORAGE_BUCKET: ${OBJECT_STORAGE_BUCKET:-}
    expose: ["8000"]
    networks: [edge, internal, data]
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3)"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 30s
    restart: unless-stopped

  genesis:
    image: ${GENESIS_IMAGE:?GENESIS_IMAGE wajib diisi}
    environment:
      APP_ENV: staging
      APP_HOST: 0.0.0.0
      APP_PORT: 8100
      ALOS_BACKEND_BASE_URL: http://backend:8000
      ALOS_INTERNAL_TOKEN: ${GENESIS_INTERNAL_TOKEN:?GENESIS_INTERNAL_TOKEN wajib diisi}
      ALOS_CONTRACTS_PATH: /contracts
      OTEL_SERVICE_NAME: genesis-ai-staging
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
    expose: ["8100"]
    networks: [internal]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8100/health', timeout=3)"]
      interval: 15s
      timeout: 5s
      retries: 8
      start_period: 30s
    restart: unless-stopped

  postgres:
    image: ${POSTGRES_IMAGE:-pgvector/pgvector:pg16}
    environment:
      POSTGRES_DB: ${POSTGRES_DB:?POSTGRES_DB wajib diisi}
      POSTGRES_USER: ${POSTGRES_USER:?POSTGRES_USER wajib diisi}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD wajib diisi}
    expose: ["5432"]
    networks: [data]
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ../../database/postgres/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    restart: unless-stopped

  otel-collector:
    image: ${OTEL_COLLECTOR_IMAGE:-otel/opentelemetry-collector-contrib:0.135.0}
    command: ["--config=/etc/otelcol/config.yaml"]
    expose: ["4317", "4318"]
    networks: [internal]
    volumes:
      - ../../observability/otel/collector-config.yaml:/etc/otelcol/config.yaml:ro
    restart: unless-stopped

networks:
  edge:
  internal:
    internal: true
  data:
    internal: true

volumes:
  caddy-data:
  caddy-config:
  postgres-data:
