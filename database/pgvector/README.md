# pgvector

Compose memakai image `pgvector/pgvector` dan initialization mengaktifkan extension `vector`.
Index, dimension, distance operator, retention, serta migration vector table dimiliki aplikasi
yang memiliki data tersebut.
